{
  "name": "vorsorge-wizard",
  "version": "1.0.0",
  "private": true,
  "description": "Vorsorge Wizard — Create advance directive documents for Germany",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend && npm run build --prefix backend",
    "db:setup": "npm run db:generate --prefix backend && npm run db:migrate --prefix backend",
    "db:generate": "npm run db:generate --prefix backend",
    "docker:dev": "docker compose -f docker/docker-compose.yml up --build",
    "install:all": "npm install && npm install --prefix frontend && npm install --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
